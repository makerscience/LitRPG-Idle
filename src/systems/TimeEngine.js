// TimeEngine — stub for Phase 1. Phase 2 fills in tick loop + offline calc.

const TimeEngine = {
  init() {
    // no-op — placeholder for boot sequence
  },

  destroy() {
    // no-op
  },
};

export default TimeEngine;
