// Feature gates for the vertical slice (Areas 1-3).
// Disabled systems are not booted and not surfaced in UI.

export const FEATURES = {
  prestigeEnabled: false,
  territoryEnabled: false,
  townEnabled: false,
  cheatsEnabled: false,
};
